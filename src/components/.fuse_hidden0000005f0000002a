// This component has been merged into Buddy.tsx (the "Mango" helper),
// which now includes both the chat and the guided tour in a single
// bottom-right floating panel. This file is kept only so nothing breaks
// if something still imports it, but it is no longer used by the app.
export {};
