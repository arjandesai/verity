// Superseded by the animated sun/moon toggle in "@/components/ui/theme-toggle".
// Kept as a re-export so nothing breaks if something still imports from here.
export { ThemeToggle } from "@/components/ui/theme-toggle";
