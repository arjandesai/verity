import * as React from "react";

export function Footer() {
  return (
    <footer className="border-t border-border mt-24">
      <div className="container py-8 flex flex-col md:flex-row items-center justify-between gap-3 text-sm text-text-soft">
        <span>© {new Date().getFullYear()} Verity. A cognitive screening demo, not a medical diagnosis.</span>
        <span>Built with React + Tailwind</span>
      </div>
    </footer>
  );
}
